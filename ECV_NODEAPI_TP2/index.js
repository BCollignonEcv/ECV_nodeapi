const { bc_slug, bc_upperWord } = require('./string.js');
const { bc_alphabeticSort, bc_randomElement } = require('./array.js');

module.exports = {
    bc_slug,
    bc_upperWord,
    bc_alphabeticSort,
    bc_randomElement
}